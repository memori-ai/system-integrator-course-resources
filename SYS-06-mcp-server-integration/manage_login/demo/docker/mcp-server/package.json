{
  "name": "mcp-filesystem-server",
  "version": "1.0.0",
  "description": "MCP Filesystem Server for Demo 3 - exposes file operations on a shared workspace directory",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0"
  }
}
